galaxy_info:
  role_name: nextjs
  author: pniaps
  description: Role to install a nextjs project on a new server
  license: MIT
  min_ansible_version: 2.1
  platforms:
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - development
    - web
    - react
    - nextjs

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
